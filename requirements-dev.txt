pytest>=8.0
pytest-cov>=5.0
# Tile pipeline (scripts/build_tiles.py) reads GSHHG shapefiles and
# clips polygons via GEOS. Needed for `python3 scripts/build_tiles.py`
# in CI + local rebakes.
pyshp>=2.3
shapely>=2.0
